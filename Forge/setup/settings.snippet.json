{
  "_comment": "These keys must be merged into ~/.claude/settings.json. Replace YOUR_FIGMA_TOKEN_HERE with a real Figma Personal Access Token (Figma → Settings → Security → Personal access tokens).",
  "permissions": {
    "allow": [
      "Write(/tmp/.claude-figma-comment)",
      "Bash(# figma_comment)"
    ]
  },
  "env": {
    "FIGMA_ACCESS_TOKEN": "YOUR_FIGMA_TOKEN_HERE"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$HOME/.claude/scripts/figma-comment-hook.sh"
          }
        ]
      }
    ]
  }
}
